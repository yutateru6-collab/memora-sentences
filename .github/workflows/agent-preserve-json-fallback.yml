name: Preserve legacy JSON fallback

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-mobile-reader-welcome
          fetch-depth: 0
      - name: Preserve existing JSON failure behavior
        run: |
          python <<'PY'
          from pathlib import Path
          path = Path('App.tsx')
          text = path.read_text()
          old = '''           } catch(e) { console.error("JSON parse error", e); }
      }

      parsedCards = text.split('\\n').filter(l => l.trim()).map((line, i) => {
'''
          new = '''           } catch(e) { console.error("JSON parse error", e); }
           return parsedCards;
      }

      parsedCards = text.split('\\n').filter(l => l.trim()).map((line, i) => {
'''
          assert old in text
          path.write_text(text.replace(old, new, 1))
          PY
          npm install
          npm run build
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add App.tsx
          git rm .github/workflows/agent-preserve-json-fallback.yml
          git commit -m "fix: preserve legacy JSON parse failure behavior"
          git push origin HEAD:agent/fix-mobile-reader-welcome
